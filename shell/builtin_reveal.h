#ifndef BUILTIN_REVEAL_H
#define BUILTIN_REVEAL_H

/*
 * builtin_reveal.c  --  reveal command (B.2)
 *
 * Syntax: reveal (-(a | l)*)* (~ | . | .. | - | name)?
 *
 * Lists directory contents. Flags:
 *   -a : show hidden files (those starting with .)
 *   -l : one entry per line
 * Without -l, entries are printed in ls-style format.
 */

#include "shell.h"

#endif /* BUILTIN_REVEAL_H */
